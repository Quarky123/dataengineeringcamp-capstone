version: 2

sources:
  - name: src_aapl
    tables:
      - name: src_aapl
        columns:
          - name: SYMBOL
            tests:
              - custom_tests.test_unique_not_null:
                  column_name: SYMBOL
          - name: DATE
            tests:
              - custom_tests.test_unique_not_null:
                  column_name: DATE
          # Add tests for other columns as needed
  - name: src_msft
    tables:
      - name: src_msft
        columns:
          - name: SYMBOL
            tests:
              - custom_tests.test_unique_not_null:
                  column_name: SYMBOL
          - name: DATE
            tests:
              - custom_tests.test_unique_not_null:
                  column_name: DATE
          # Add tests for other columns as needed
  - name: src_tsla
    tables:
      - name: src_tsla
        columns:
          - name: SYMBOL
            tests:
              - custom_tests.test_unique_not_null:
                  column_name: SYMBOL
          - name: DATE
            tests:
              - custom_tests.test_unique_not_null:
                  column_name: DATE
          # Add tests for other columns as needed
